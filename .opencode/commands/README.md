# OpenCode Commands

This directory is reserved for future project-specific OpenCode commands.

No custom commands are enabled yet.

Reason:

- the repository is still in bootstrap mode
- security-sensitive shortcuts should only be added after backend workflows are stable
